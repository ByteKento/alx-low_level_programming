#include "main.h"
#include<stdio.h>
/**
 * main - Entry point
 *_puts - return string
 *@str: provided string
 * Return: Always 0 (Success)
 */
void _puts(char *str);

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
