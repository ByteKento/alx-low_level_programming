#include "main.h"

/**
 *strlen - prints half of a string
 *@s: string character.
 *Return: m(success)
 */

void puts_half(char *str)
{
	int m = 0;

	while (*s != '\0')
	{
		m++;
		str++;
	}
	return (m);
}
